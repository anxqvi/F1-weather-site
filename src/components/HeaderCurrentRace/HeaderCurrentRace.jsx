import React from 'react';
import styles from './HeaderCurrentRace.module.css';

// Header that is displayed on PageCurrentRace
const HeaderCurrentRace = () => (
    <h1 className={styles.heading}>
        Current Grand Prix
    </h1>
);

export default HeaderCurrentRace;
