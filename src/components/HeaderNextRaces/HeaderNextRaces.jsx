import React from 'react';
import styles from './HeaderNextRaces.module.css';

// Header that is displayed on PageNextRace1
const HeaderNextRaces = () => (
    <h1 className={styles.heading}>
        Next Grands Prix
    </h1>
);

export default HeaderNextRaces;
